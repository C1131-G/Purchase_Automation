VITE_API_URL=http://localhost:4000
# Must match backend DEFAULT_CURRENCY_CODE — fallback when OADM/admin or DocCurr is "$"/empty
VITE_DEFAULT_CURRENCY_CODE=FJD
# SESSION_SECRET and PORTAL_PASSWORD_SALT_ROUNDS belong in hana-backend/.env, not here.