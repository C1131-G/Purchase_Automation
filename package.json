{
  "name": "vendor-portal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rimraf frontend/dist hana-backend/dist sql-backend/dist",
    "kill:ports:frontend": "node scripts/kill-port.js frontend",
    "kill:ports:backend": "node scripts/kill-port.js backend",
    "kill:ports:all": "node scripts/kill-port.js all",
    "dev:hana-backend": "pnpm kill:ports:backend && cd hana-backend && pnpm dev",
    "dev:sql-backend": "pnpm kill:ports:backend && cd sql-backend && pnpm dev",
    "dev:frontend": "pnpm kill:ports:frontend && cd frontend && pnpm dev",
    "build:hana-backend": "cd hana-backend && pnpm build",
    "build:sql-backend": "cd sql-backend && pnpm build",
    "build:frontend": "cd frontend && pnpm build",
    "start:frontend": "pnpm kill:ports:frontend && cd frontend && pnpm preview",
    "start:hana-backend": "pnpm kill:ports:backend && cd hana-backend && pnpm start",
    "start:sql-backend": "pnpm kill:ports:backend && cd sql-backend && pnpm start",
    "build": "pnpm clean && pnpm check && pnpm build:hana-backend && pnpm build:sql-backend && pnpm build:frontend",
    "test": "cd hana-backend && pnpm test",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt",
    "format:check": "oxfmt check",
    "check": "pnpm lint:fix && pnpm format",
    "fix": "pnpm lint:fix && pnpm format",
    "prepare": "husky"
  },
  "devDependencies": {
    "drizzle-orm": "^0.45.2",
    "husky": "^9.1.7",
    "oxfmt": "^0.56.0",
    "oxlint": "^1.71.0",
    "rimraf": "^6.1.3"
  },
  "packageManager": "pnpm@11.8.0"
}
